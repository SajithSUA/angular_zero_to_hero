export interface UserDetail {

    //example object in angular (this is initialize object in angular)
    name:string;
    email:string;
    phoneNo:string;

    // not requierd variables initialise this way
    mothername?:string;
}
